import 'package:google_fonts/google_fonts.dart';

class productos {
  final id;
  final nombre;
  final imagen;
  int cantidad;
  int precio;

  productos(this.id, this.nombre, this.imagen, this.cantidad, this.precio) ;
}
